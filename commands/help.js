module.exports = {
	name: 'help',
	description: 'Gives a list of available commands',
	execute(message, args) {
		message.channel.send('```Everything is not implemented yet so the help will come later, once more commands are set up :)```');
	},
};